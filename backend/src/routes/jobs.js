// src/routes/jobs.js
const express = require('express');

const router = express.Router();

// All job routes are handled in admin routes
// This file is kept for future job-specific endpoints that might be needed

module.exports = router;